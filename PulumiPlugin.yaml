runtime: yaml
name: random-string-component
version: 0.1.1
keywords:
  - component
components:
  RandomString:
    inputs:
      length:
        type: integer
    resources:
      random:
        type: command:local:Command
        properties:
          create: "openssl rand -hex ${length}"
    outputs:
      rand: "The random string output is ${random.stdout}"
